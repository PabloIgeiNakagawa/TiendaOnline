@using TiendaOnline.Domain.Entities
@model TiendaOnline.Domain.Entities.Usuario
@{
    ViewData["Title"] = "Registrarse";
}
@section Styles {
    <link rel="stylesheet" href="~/css/Admin/Usuario/CrearUsuario.css" asp-append-version="true" />
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-xl-6">
            <!-- Encabezado -->
            <div class="text-center mb-5">
                <div class="d-flex align-items-center justify-content-center mx-auto mb-3 text-white fs-2 rounded-4 bg-primary " style="width: 80px; height: 80px;">
                    <i class="bi bi-person-plus-fill"></i>
                </div>
                <h2 class="fw-bold mb-2">Crear Usuario</h2>
                <p class="text-muted">Complete la información para crear un nuevo usuario en el sistema</p>
            </div>

            <!-- Formulario -->
            <div class="shadow border-0 rounded-4 overflow-hidden">
                <div class="p-5">
                    <form asp-area="Admin" asp-controller="Usuario" asp-action="CrearUsuario" method="post" id="formularioRegistro" novalidate>
                        @Html.AntiForgeryToken()

                        <!-- Información Personal -->
                        <div class="position-relative mb-4">
                            <h6 class="border-bottom mb-3 text-secondary fw-semibold small text-uppercase pb-2" style="letter-spacing: 0.5px;">
                                <i class="bi bi-person-circle me-2"></i>Información Personal
                            </h6>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Nombre" class="form-control border-1" id="campoNombre" placeholder="Nombre" required />
                                        <label for="campoNombre">
                                            <i class="bi bi-person me-1"></i>Nombre
                                        </label>
                                        <span asp-validation-for="Nombre" class="text-danger small"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Apellido" class="form-control" id="campoApellido" placeholder="Apellido" required />
                                        <label for="campoApellido">
                                            <i class="bi bi-person me-1"></i>Apellido
                                        </label>
                                        <span asp-validation-for="Apellido" class="text-danger small"></span>
                                    </div>
                                </div>
                            </div>
                            <div class="row g-3 mt-1">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="FechaNacimiento" class="form-control" id="campoFechaNacimiento" type="date" placeholder="Fecha de Nacimiento" required />
                                        <label for="campoFechaNacimiento">
                                            <i class="bi bi-calendar me-1"></i>Fecha de Nacimiento
                                        </label>
                                        <span asp-validation-for="FechaNacimiento" class="text-danger small"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Telefono" class="form-control" id="campoTelefono" placeholder="Teléfono" required />
                                        <label for="campoTelefono">
                                            <i class="bi bi-telephone me-1"></i>Teléfono
                                        </label>
                                        <span asp-validation-for="Telefono" class="text-danger small"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Información de Contacto -->
                        <div class="seccion-formulario mb-4">
                            <h6 class="border-bottom mb-3 text-secondary fw-semibold small text-uppercase pb-2" style="letter-spacing: 0.5px;">
                                <i class="bi bi-envelope-at me-2"></i>Información de Contacto
                            </h6>
                            <div class="row g-3">
                                <div class="col-12">
                                    <div class="form-floating">
                                        <input asp-for="Email" class="form-control" id="campoEmail" type="email" placeholder="Correo Electrónico" required />
                                        <label for="campoEmail">
                                            <i class="bi bi-envelope me-1"></i>Correo Electrónico
                                        </label>
                                        <span asp-validation-for="Email" class="text-danger small d-block mt-1"></span>
                                    </div>
                                </div>
                                <div class="col-12">
                                    <div class="form-floating">
                                        <input asp-for="Direccion" class="form-control" id="campoDireccion" placeholder="Dirección" required />
                                        <label for="campoDireccion">
                                            <i class="bi bi-geo-alt me-1"></i>Dirección
                                        </label>
                                        <span asp-validation-for="Direccion" class="text-danger small d-block mt-1"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Seguridad -->
                        <div class="seccion-formulario mb-4">
                            <h6 class="border-bottom mb-3 text-secondary fw-semibold small text-uppercase pb-2" style="letter-spacing: 0.5px;">
                                <i class="bi bi-shield-lock me-2"></i>Seguridad
                            </h6>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Contrasena" type="password" class="form-control" id="campoContrasena" placeholder="Contraseña" required />
                                        <label for="campoContrasena">
                                            <i class="bi bi-lock me-1"></i>Contraseña
                                        </label>
                                        <span asp-validation-for="Contrasena" class="text-danger small d-block mt-1"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="ConfirmarContrasena" type="password" class="form-control" id="campoConfirmarContrasena" placeholder="Confirmar Contraseña" required />
                                        <label for="campoConfirmarContrasena">
                                            <i class="bi bi-lock-fill me-1"></i>Confirmar Contraseña
                                        </label>
                                        <span asp-validation-for="ConfirmarContrasena" class="text-danger small d-block mt-1"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="seccion-formulario mb-4">
                            <h6 class="border-bottom mb-3 text-secondary fw-semibold small text-uppercase pb-2" style="letter-spacing: 0.5px;">
                                <i class="bi bi-shield-check me-2"></i>Configuración de Acceso
                            </h6>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <select asp-for="Rol" class="form-select" id="campoRol" asp-items="Html.GetEnumSelectList<Rol>()" required>
                                            <option value="" disabled selected>Seleccione un rol</option>
                                        </select>
                                        <label for="campoRol">
                                            <i class="bi bi-shield me-1"></i>Rol del Usuario
                                        </label>
                                        <span asp-validation-for="Rol" class="text-danger small"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="alert alert-info border-0 h-100 d-flex align-items-center">
                                        <div>
                                            <i class="bi bi-info-circle me-2"></i>
                                            <strong>Roles:</strong><br>
                                            <small>
                                                • <strong>Usuario:</strong> Acceso básico<br>
                                                • <strong>Administrador:</strong> Acceso completo
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Botones -->
                        <div class="d-lg-flex justify-content-between gap-2 align-items-center pt-3 border-top">
                            <button type="submit" class="btn btn-primary fw-medium rounded-3 p-3 my-2 my-lg-0" id="botonEnviar">
                                <i class="bi bi-check-circle me-2"></i>
                                <span class="texto-boton">Crear Usuario</span>
                                <span class="spinner-border spinner-border-sm ms-2 d-none" role="status" aria-hidden="true"></span>
                            </button>
                            <a class="btn btn-outline-secondary fw-medium rounded-3 p-3 my-2 my-lg-0 d-block" onclick="limpiarFormulario()">
                                <i class="bi bi-arrow-clockwise me-2"></i>Limpiar
                            </a>
                            <a asp-area="Admin" asp-controller="Usuario" asp-action="Index" class="btn btn-outline-danger fw-medium rounded-3 p-3 d-block my-2 my-lg-0">
                                <i class="bi bi-x-circle me-1"></i>Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const formulario = document.getElementById('formularioRegistro');
            const botonEnviar = document.getElementById('botonEnviar');

            // Validación en tiempo real
            const entradas = formulario.querySelectorAll('input, select');
            entradas.forEach(entrada => {
                entrada.addEventListener('blur', function () {
                    validarCampo(this);
                });

                entrada.addEventListener('input', function () {
                    if (this.classList.contains('is-invalid')) {
                        validarCampo(this);
                    }
                });
            });

            function validarCampo(campo) {
                const esValido = campo.checkValidity();
                campo.classList.remove('is-valid', 'is-invalid');

                if (campo.value.trim() !== '') {
                    campo.classList.add(esValido ? 'is-valid' : 'is-invalid');
                }
            }

            // Capitalización automática
            const camposNombre = ['campoNombre', 'campoApellido'];
            camposNombre.forEach(id => {
                const entrada = document.getElementById(id);
                if (entrada) {
                    entrada.addEventListener('input', function (e) {
                        const palabras = e.target.value.split(' ');
                        const palabrasCapitalizadas = palabras.map(palabra =>
                            palabra.charAt(0).toUpperCase() + palabra.slice(1).toLowerCase()
                        );
                        e.target.value = palabrasCapitalizadas.join(' ');
                    });
                }
            });

            // Función para limpiar formulario
            window.limpiarFormulario = function () {
                formulario.reset();
                entradas.forEach(entrada => {
                    entrada.classList.remove('is-valid', 'is-invalid');
                });
            };
        });
    </script>
}

